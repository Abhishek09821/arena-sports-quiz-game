import Link from "next/link";
import { FAQS } from "@/data/site";
export default function Methodology() {
  return <main className="arena-container methodology-page"><p className="arena-eyebrow">Behind the questions</p><h1>Good games start with good questions.</h1><p className="page-intro">Here’s how Arena prepares your round, and where AI still has limits.</p><div className="methodology-grid">
    {[["01", "Your selection comes first", "We use your sport, tournament, decade, athlete and difficulty when preparing questions. All Sports includes our four sports only; General Knowledge stays separate."], ["02", "A complete round", "Each question needs four distinct choices and a matching correct answer. Rejected questions are replaced before the round starts. Mixed distributes questions as evenly as the round size allows."], ["03", "History and factual review", "Signed-in history helps prevent repeats across rounds and devices. AI questions receive a separate factual review with web evidence. These checks reduce errors, but cannot guarantee that every fact or paraphrase is caught."], ["04", "A clear limit", "If the provider is unavailable or too few questions pass review, the round does not start. We never silently shorten a round or relax your filters to fill it."]].map(([number,title,text]) => <section className="arena-card" key={number}><span className="arena-eyebrow">{number}</span><h2>{title}</h2><p>{text}</p></section>)}
  </div><section className="methodology-faq" id="faqs"><h2>Frequently asked questions</h2>{FAQS.map(faq=><details key={faq.question}><summary>{faq.question}</summary><p>{faq.answer}</p></details>)}</section><Link href="/challenge" className="arena-btn arena-btn-primary">Build a challenge</Link></main>;
}
